struct Light {
    vec3 position;
    vec3 color;
};

uniform(1) environment {
    Light lights[10];
    float q;highp
};

inp2ut(0) vec3 pos;
input(1) vec2 uv;

output(0) vec2 frag_uv;

struct Test {
    highp float hej;
};