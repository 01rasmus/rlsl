struct Light {
    vec3 position;
    vec3 color;
};

uniform(1) environment {
    Light lights[40];
};

input(0) vec3 pos;
input(1) vec2 uv;

output(0) vec2 frag_uv;

struct Test {
    1.0 float hej;
};